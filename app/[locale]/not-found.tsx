import NotFoundHero from '@/components/sections/NotFoundHero';

// Note: This page is rendered when notFound() is called within a [locale] route.
export default function NotFoundPage() {
  return <NotFoundHero />;
}
